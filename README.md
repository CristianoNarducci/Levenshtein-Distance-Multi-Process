# Implementation of Multi Process Version of String Searching and compare with sequential version

An implementation of string searching using two different algorithms for parallel computing course:
* Levenshtein Distance
* Bitap Search

using a built-in string generator or a real sentences dataset.

## Requirements
* Python >= 3.10
* tqdm
